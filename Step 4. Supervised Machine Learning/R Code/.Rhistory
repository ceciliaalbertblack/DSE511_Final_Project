# Plot the histogram
hist(clean_df$Deaths,
freq = FALSE,                   #freq = FALSE for density
main = "Poisson Distribution of Deaths (lambda = 0.0373)",
xlab = "Number of Events",
ylab = "Density")
# Overlay discrete Poisson probabilities
x_vals <- 0:max(clean_df$Deaths)
points(x_vals, dpois(x_vals, death_lambda), col = "red", pch = 16)
lines(x_vals, dpois(x_vals, death_lambda), col = "red", lwd = 2)
mean(clean_df$Illnesses)
var(clean_df$Illnesses)
mean(clean_df$Hospitalizations)
var(clean_df$Hospitalizations)
mean(clean_df$Deaths)
var(clean_df$Deaths)
library(MASS)
ill_nb <- fitdistr(clean_df$Illnesses, "Negative Binomial")
# Isolate standardized numeric variables for cluster analysis
num_vars <- c(
"Illnesses_std", "Hospitalizations_std", "Deaths_std",
"Hosp_per_ill", "Death_per_ill"
)
# Calculate distance matrix
dist_matrix <- dist(num_vars, method = "euclidean")
# Perform hierarchical clustering
hc_result <- hclust(dist_matrix, method = "ward.D2")
?hclust
# Isolate standardized numeric variables for cluster analysis
num_vars <- clean_df %>%
select(
Illnesses_std,
Hospitalizations_std,
Deaths_std,
Hosp_per_ill,
Death_per_ill
)
# Isolate standardized numeric variables for cluster analysis
num_vars <- clean_df %>%
dplyr::select(
Illnesses_std,
Hospitalizations_std,
Deaths_std,
Hosp_per_ill,
Death_per_ill
)
# Calculate distance matrix
dist_matrix <- dist(num_vars, method = "euclidean")
# Perform hierarchical clustering
hc_result <- hclust(dist_matrix, method = "ward.D2")
set.seed(123)
k <- 4  # choose a starting number of clusters
kmeans_result <- kmeans(num_vars, centers = k, nstart = 25)
# Attach cluster labels back to your data
clean_df$kmeans_cluster <- kmeans_result$cluster
table(clean_df$kmeans_cluster)
clean_df %>%
group_by(kmeans_cluster) %>%
summarise(
mean_ill = mean(Illnesses_std),
mean_hosp = mean(Hospitalizations_std),
mean_death = mean(Deaths_std),
.groups = "drop"
)
table(clean_df$kmeans_cluster)
clean_df %>%
group_by(kmeans_cluster) %>%
summarise(
median_ill = median(Illnesses_std),
median_hosp = median(Hospitalizations_std),
median_death = median(Deaths_std),
.groups = "drop"
)
table(clean_df$kmeans_cluster)
clean_df %>%
group_by(kmeans_cluster) %>%
summarise(
mean_ill = mean(Illnesses_std),
mean_hosp = mean(Hospitalizations_std),
mean_death = mean(Deaths_std),
.groups = "drop"
)
library(readr)
library(tidyverse)
library(fitdistrplus)
library(MASS)
ill_nb <- fitdistr(clean_df$Illnesses, "Negative Binomial")
ill_nb <- fitdistr(clean_df$Illnesses, "Binomial")
?fitdistr
ill_nb <- fitdistr(clean_df$Illnesses, "negative binomial")
num_vars <- clean_df %>%
dplyr::select(
Illnesses_std,
Hospitalizations_std,
Deaths_std,
Hosp_per_ill,
Death_per_ill
)
cor(num_vars)
pairs(num_vars)
pca_result <- prcomp(num_vars, scale. = TRUE)
summary(pca_result)
plot(pca_result, type = "l")  # Scree plot
pca_scores <- as.data.frame(pca_result$x[, 1:3])
pca_scores <- as.data.frame(pca_result$x[, 1:3])
pca_scores
library(readr)
library(tidyverse)
library(fitdistrplus)
library(factoextra)
fviz_nbclust(pca_scores, kmeans, method = "wss")       # Elbow method
clusters <- fviz_nbclust(pca_scores, kmeans, method = "wss")       # Elbow method
fviz_nbclust(pca_scores, kmeans, method = "silhouette") # Silhouette method
set.seed(123)
kmeans_result <- kmeans(pca_scores, centers = 3, nstart = 25)
clean_df$cluster <- factor(kmeans_result$cluster)
fviz_cluster(kmeans_result, data = pca_scores, geom = "point", ellipse = TRUE)
clean_df %>%
group_by(cluster) %>%
summarise(across(
c(Illnesses_std, Hospitalizations_std, Deaths_std, Hosp_per_ill, Death_per_ill),
mean, na.rm = TRUE
))
ggplot(clean_df, aes(x = Etiology, fill = cluster)) +
geom_bar(position = "fill") +
coord_flip() +
labs(title = "Cluster composition by Etiology", y = "Proportion")
# 1. Identify top 30 most common etiologies
top_etiologies <- clean_df %>%
count(Etiology, sort = TRUE) %>%
slice_head(n = 30) %>%
pull(Etiology)
# 2. Filter dataset to those etiologies
df_top <- clean_df %>%
filter(Etiology %in% top_etiologies)
# 3. Reorder Etiology factor by frequency
df_top <- df_top %>%
mutate(Etiology = forcats::fct_infreq(Etiology))
# 4. Plot
ggplot(df_top, aes(x = Etiology, fill = cluster)) +
geom_bar(position = "fill") +
coord_flip() +
labs(
title = "Cluster Composition by Top 30 Etiologies",
y = "Proportion",
x = "Etiology"
) +
theme_minimal() +
theme(
legend.position = "bottom",
axis.text.y = element_text(size = 8)
)
unique(clean_df$Etiology_group)
ggplot(df_top, aes(x = Etiology_group, fill = cluster)) +
geom_bar(position = "fill") +
coord_flip() +
labs(
title = "Cluster Composition by Etiology Group",
y = "Proportion",
x = "Etiology_group"
) +
theme_minimal() +
theme(
legend.position = "bottom",
axis.text.y = element_text(size = 8)
)
# 1. Identify top 30 most common etiologies
top_etiologies <- clean_df %>%
count(Etiology, sort = TRUE) %>%
slice_head(n = 30) %>%
pull(Etiology)
# 2. Filter dataset to those etiologies
df_top <- clean_df %>%
filter(Etiology %in% top_etiologies)
# 3. Reorder Etiology factor by frequency
df_top <- df_top %>%
mutate(Etiology = forcats::fct_infreq(Etiology))
# 4. Plot
ggplot(df_top, aes(x = Etiology, fill = cluster)) +
geom_bar(position = "fill") +
coord_flip() +
labs(
title = "Cluster Composition by Top 30 Etiologies",
y = "Proportion",
x = "Etiology"
) +
theme_minimal() +
theme(
legend.position = "bottom",
axis.text.y = element_text(size = 8)
)
# 1. Identify top 50 etiologies causing illnesses
top_etiologies <- clean_df %>%
group_by(Etiology) %>%
summarise(total_illnesses = sum(Illnesses, na.rm = TRUE)) %>%
arrange(desc(total_illnesses)) %>%
slice_head(n = 50)
# 2. Filter dataset to those etiologies
df_top <- clean_df %>%
filter(Etiology %in% top_etiologies)
# 3. Reorder Etiology factor by frequency
df_top <- df_top %>%
mutate(Etiology = forcats::fct_infreq(Etiology))
# 4. Plot
ggplot(df_top, aes(x = Etiology, fill = cluster)) +
geom_bar(position = "fill") +
coord_flip() +
labs(
title = "Cluster Composition by Top 30 Etiologies",
y = "Proportion",
x = "Etiology"
) +
theme_minimal() +
theme(
legend.position = "bottom",
axis.text.y = element_text(size = 8)
)
# 1. Identify top 50 etiologies causing illnesses
top_etiologies <- clean_df %>%
group_by(Etiology) %>%
summarise(total_illnesses = sum(Illnesses, na.rm = TRUE), .groups = "drop") %>%
arrange(desc(total_illnesses)) %>%
slice_head(n = 50) %>%
pull(Etiology)   # <- this is the key fix
# 2. Filter dataset to those etiologies
df_top <- clean_df %>%
filter(Etiology %in% top_etiologies)
# 3. Reorder Etiology factor by frequency
df_top <- df_top %>%
mutate(Etiology = forcats::fct_infreq(Etiology))
# 4. Plot
ggplot(df_top, aes(x = Etiology, fill = cluster)) +
geom_bar(position = "fill") +
coord_flip() +
labs(
title = "Cluster Composition by Top 30 Etiologies",
y = "Proportion",
x = "Etiology"
) +
theme_minimal() +
theme(
legend.position = "bottom",
axis.text.y = element_text(size = 8)
)
# 1. Identify top 50 settings causing illnesses
top_settings <- clean_df %>%
group_by(Setting) %>%
summarise(total_illnesses = sum(Illnesses, na.rm = TRUE), .groups = "drop") %>%
arrange(desc(total_illnesses)) %>%
slice_head(n = 50) %>%
pull(Setting)   # <- this is the key fix
# 2. Filter dataset to those etiologies
df_top <- clean_df %>%
filter(Setting %in% top_settings)
# 3. Reorder Etiology factor by frequency
df_top <- df_top %>%
mutate(Setting = forcats::fct_infreq(Setting))
# 4. Plot
ggplot(df_top, aes(x = Setting, fill = cluster)) +
geom_bar(position = "fill") +
coord_flip() +
labs(
title = "Cluster Composition by Top 50 Settings",
y = "Proportion",
x = "Setting"
) +
theme_minimal() +
theme(
legend.position = "bottom",
axis.text.y = element_text(size = 8)
)
# 1. Identify top primary modes causing illnesses
top_mode <- clean_df %>%
group_by(Primary_mode) %>%
summarise(total_illnesses = sum(Illnesses, na.rm = TRUE), .groups = "drop") %>%
arrange(desc(total_illnesses)) %>%
pull(Primary_mode)   # <- this is the key fix
# 2. Filter dataset to those Primary_mode
df_top <- clean_df %>%
filter(Primary_mode %in% top_mode)
# 3. Reorder Primary_mode factor by frequency
df_top <- df_top %>%
mutate(Primary_mode = forcats::fct_infreq(Primary_mode))
# 4. Plot
ggplot(df_top, aes(x = Primary_mode, fill = cluster)) +
geom_bar(position = "fill") +
coord_flip() +
labs(
title = "Cluster Composition by Primary Mode",
y = "Proportion",
x = "Primary_mode"
) +
theme_minimal() +
theme(
legend.position = "bottom",
axis.text.y = element_text(size = 8)
)
top_mode
df_top
mode_table <- clean_df %>%
group_by(Primary_mode) %>%
summarise(
total_illnesses = sum(Illnesses, na.rm = TRUE),
mean_illnesses = mean(Illnesses, na.rm = TRUE),
n_outbreaks = n(),
.groups = "drop"
) %>%
mutate(
prop_of_total = total_illnesses / sum(total_illnesses)
) %>%
arrange(desc(total_illnesses))
# View nicely
mode_table
# Base R version
mode_cluster_tab <- table(clean_df$Primary_mode, clean_df$cluster)
mode_cluster_tab
pca_clust <- fviz_cluster(kmeans_result, data = pca_scores, geom = "point", ellipse = TRUE)
setwd("C:/Users/q4q/repos/DSE511_Final_Project/Step 3. Cluster Analysis/R Code")
library(readr)
library(tidyverse)
library(fitdistrplus)
library(factoextra)
clean_df <- read_csv("C:/Users/q4q/repos/DSE511_Final_Project/Step 1. Data Cleaning/R code/outbreak_clean_r_v2.csv")
head(clean_df)
num_vars <- clean_df %>%
dplyr::select(
Illnesses_std,
Hospitalizations_std,
Deaths_std,
Hosp_per_ill,
Death_per_ill
)
cor(num_vars)
pairs(num_vars)
pca_result <- prcomp(num_vars, scale. = TRUE)
summary(pca_result)
plot(pca_result, type = "l")  # Scree plot
pca_scores <- as.data.frame(pca_result$x[, 1:3])
pca_scores
set.seed(123)
kmeans_result <- kmeans(pca_scores, centers = 3, nstart = 25)
clean_df$cluster <- factor(kmeans_result$cluster)
pca_clust <- fviz_cluster(kmeans_result, data = pca_scores, geom = "point", ellipse = TRUE)
pca_clust
# Save the plot
ggsave(
filename = "PCA_kmeans_clusters.png",  # File name and extension
plot = pca_clust,                      # Plot object to save
width = 8,                             # Width in inches
height = 6,                            # Height in inches
dpi = 300                              # Resolution (good for publication)
)
# 1. Identify top primary modes causing illnesses
top_mode <- clean_df %>%
group_by(Primary_mode) %>%
summarise(total_illnesses = sum(Illnesses, na.rm = TRUE), .groups = "drop") %>%
arrange(desc(total_illnesses)) %>%
pull(Primary_mode)   # <- this is the key fix
# 2. Filter dataset to those Primary_mode
df_top <- clean_df %>%
filter(Primary_mode %in% top_mode)
# 3. Reorder Primary_mode factor by frequency
df_top <- df_top %>%
mutate(Primary_mode = forcats::fct_infreq(Primary_mode))
# 4. Plot
ggplot(df_top, aes(x = Primary_mode, fill = cluster)) +
geom_bar(position = "fill") +
coord_flip() +
labs(
title = "Cluster Composition by Primary Mode",
y = "Proportion",
x = "Primary_mode"
) +
theme_minimal() +
theme(
legend.position = "bottom",
axis.text.y = element_text(size = 8)
)
# Save the plot
ggsave(
filename = "Cluster_by_primary_mode.png",  # File name and extension
width = 8,                             # Width in inches
height = 6,                            # Height in inches
dpi = 300                              # Resolution (good for publication)
)
# Base R version
mode_cluster_tab <- table(clean_df$Primary_mode, clean_df$cluster)
mode_cluster_tab
# 1. Identify top 50 etiologies causing illnesses
top_etiologies <- clean_df %>%
group_by(Etiology) %>%
summarise(total_illnesses = sum(Illnesses, na.rm = TRUE), .groups = "drop") %>%
arrange(desc(total_illnesses)) %>%
slice_head(n = 50) %>%
pull(Etiology)   # <- this is the key fix
# 2. Filter dataset to those etiologies
df_top <- clean_df %>%
filter(Etiology %in% top_etiologies)
# 3. Reorder Etiology factor by frequency
df_top <- df_top %>%
mutate(Etiology = forcats::fct_infreq(Etiology))
# 4. Plot
ggplot(df_top, aes(x = Etiology, fill = cluster)) +
geom_bar(position = "fill") +
coord_flip() +
labs(
title = "Cluster Composition by Top 50 Etiologies",
y = "Proportion",
x = "Etiology"
) +
theme_minimal() +
theme(
legend.position = "bottom",
axis.text.y = element_text(size = 8)
)
# Save the plot
ggsave(
filename = "Cluster_by_etiology.png",  # File name and extension
width = 8,                             # Width in inches
height = 6,                            # Height in inches
dpi = 300                              # Resolution (good for publication)
)
# 1. Identify top 50 settings causing illnesses
top_settings <- clean_df %>%
group_by(Setting) %>%
summarise(total_illnesses = sum(Illnesses, na.rm = TRUE), .groups = "drop") %>%
arrange(desc(total_illnesses)) %>%
slice_head(n = 50) %>%
pull(Setting)   # <- this is the key fix
# 2. Filter dataset to those settings
df_top <- clean_df %>%
filter(Setting %in% top_settings)
# 3. Reorder Setting factor by frequency
df_top <- df_top %>%
mutate(Setting = forcats::fct_infreq(Setting))
# 4. Plot
ggplot(df_top, aes(x = Setting, fill = cluster)) +
geom_bar(position = "fill") +
coord_flip() +
labs(
title = "Cluster Composition by Top 50 Settings",
y = "Proportion",
x = "Setting"
) +
theme_minimal() +
theme(
legend.position = "bottom",
axis.text.y = element_text(size = 8)
)
# Save the plot
ggsave(
filename = "Cluster_by_setting.png",  # File name and extension
width = 8,                             # Width in inches
height = 6,                            # Height in inches
dpi = 300                              # Resolution (good for publication)
)
ggplot(df_top, aes(x = Season, fill = cluster)) +
geom_bar(position = "fill") +
coord_flip() +
labs(
title = "Cluster Composition by Season",
y = "Proportion",
x = "Season"
) +
theme_minimal() +
theme(
legend.position = "bottom",
axis.text.y = element_text(size = 8)
)
# As a table
mode_cluster_tab <- table(clean_df$Season, clean_df$cluster)
mode_cluster_tab
# Plot
ggplot(df_top, aes(x = Year, fill = cluster)) +
geom_bar(position = "fill") +
coord_flip() +
labs(
title = "Cluster Composition by Year",
y = "Proportion",
x = "Season"
) +
theme_minimal() +
theme(
legend.position = "bottom",
axis.text.y = element_text(size = 8)
)
# As a table
mode_cluster_tab <- table(clean_df$Year, clean_df$cluster)
mode_cluster_tab
# Plot
ggplot(df_top, aes(x = Year, fill = cluster)) +
geom_bar(position = "fill") +
coord_flip() +
labs(
title = "Cluster Composition by Year",
y = "Proportion",
x = "Season"
) +
theme_minimal() +
theme(
legend.position = "bottom",
axis.text.y = element_text(size = 8)
)
# As a table
mode_cluster_tab <- table(clean_df$Year, clean_df$cluster)
mode_cluster_tab
setwd("C:/Users/q4q/repos/DSE511_Final_Project/Step 4. Supervised Machine Learning/R Code")
setwd("C:/Users/q4q/repos/DSE511_Final_Project/Step 4. Supervised Machine Learning/R Code")
library(readr)
library(tidyverse)
clean_df <- read_csv("C:/Users/q4q/repos/DSE511_Final_Project/Step 1. Data Cleaning/R code/outbreak_clean_r_v2.csv")
head(clean_df)
